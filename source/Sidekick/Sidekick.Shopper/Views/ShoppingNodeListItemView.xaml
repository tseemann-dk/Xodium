<?xml version="1.0" encoding="UTF-8"?>
<ContentView 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Class="Sidekick.Shopper.Views.ShoppingNodeListItemView">

    <StackLayout Orientation="Horizontal" Margin="4,2">
        <Image>
            <Image.Source>
                <FontImageSource x:Name="ImageSource" Glyph="{Binding Glyph}"/>
            </Image.Source>
            <Image.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding OpenCommand}"/>
            </Image.GestureRecognizers>
        </Image>
        <StackLayout Orientation="Horizontal" VerticalOptions="Center" HorizontalOptions="FillAndExpand">
            <Label Text="{Binding DisplayNumber}" HorizontalOptions="Start" MinimumWidthRequest="100"/>
            <Label Text="{Binding Text}" HorizontalOptions="StartAndExpand" LineBreakMode="TailTruncation"/>
            <Label Text="{Binding Value}" HorizontalOptions="End"/>
        </StackLayout>
    </StackLayout>

</ContentView>