<?xml version="1.0" encoding="UTF-8"?>
<ContentView 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Class="Sidekick.Shopper.Views.ComponentListItemView">

    <StackLayout Orientation="Horizontal" HorizontalOptions="StartAndExpand" Margin="4">

        <!-- Thumbnail -->
        <Image 
            HeightRequest="48" 
            WidthRequest="48" 
            Source="{Binding ThumbnailUri}"
            BackgroundColor="DarkGray"/>
        
        <!-- Details -->
        <StackLayout Orientation="Vertical" HorizontalOptions="StartAndExpand" Spacing="4">
            <Label Text="{Binding Text}" LineBreakMode="TailTruncation" FontSize="14"/>
            <StackLayout Orientation="Horizontal" Spacing="8">
                <Label FontSize="12" TextColor="Gray" Text="{Binding ComponentNumber}"/>
                <Label FontSize="12" TextColor="Gray" Text="{Binding Price}"/>
            </StackLayout>
        </StackLayout>
        
    </StackLayout>

</ContentView>