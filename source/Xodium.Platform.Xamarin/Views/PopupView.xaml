<?xml version="1.0" encoding="UTF-8"?>
<Grid 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Xodium.Platform.Xamarin.Views.PopupView"
    Padding="16"
    RowSpacing="8"
    HorizontalOptions="Center"
    VerticalOptions="Center"
    BackgroundColor="White">

    <Grid.RowDefinitions>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <ContentView Grid.Row="0" x:Name="ContentView"/>

    <Grid Grid.Row="1" x:Name="ButtonPanel" ColumnSpacing="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button x:Name="OkButton" Grid.Column="0" Text="OK"/>
        <Button x:Name="CancelButton" Grid.Column="1" Text="Cancel" Clicked="OnCancelButtonClicked"/>
    </Grid>

</Grid>