<?xml version="1.0" encoding="UTF-8"?>
<ContentView 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Class="Sidekick.Views.NodeListItemView">

    <!--
    <ContentView.Resources>
        <ResourceDictionary>
            <converters:DateTimeToStringConverter x:Key="DateTimeToStringConverter"/>
        </ResourceDictionary>
    </ContentView.Resources>
    -->
    
    <StackLayout Orientation="Horizontal">
        <!--<Label Text="{Binding Date, Converter={StaticResource DateTimeToStringConverter}, ConverterParameter='dd-MM'}" />-->
        <Label Text="{Binding DisplayNumber}" />
        <Label Text="{Binding Text}" />
        <Label Text="{Binding Value}" HorizontalOptions="EndAndExpand"/>
    </StackLayout>

    <ContentView.GestureRecognizers>
        <TapGestureRecognizer NumberOfTapsRequired="2" Command="{Binding OpenCommand}"/>
    </ContentView.GestureRecognizers>

</ContentView>