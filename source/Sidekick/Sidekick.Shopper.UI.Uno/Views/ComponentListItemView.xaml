<UserControl
    x:Class="Sidekick.Shopper.UI.Uno.Views.ComponentListItemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sidekick.Shopper.UI.Uno.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="56"
    d:DesignWidth="400">

    <StackPanel Orientation="Horizontal" Margin="4" Spacing="8">

        <!-- Thumbnail -->
        <Grid Height="48" Width="48" Background="Gray">
            <Image Source="{Binding ThumbnailUri}"/>
        </Grid>

        <!-- Details -->
        <StackPanel Orientation="Vertical" Spacing="4">
            <TextBlock Text="{Binding Text}" FontSize="14"/>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock FontSize="12" Foreground="Gray" Text="{Binding ComponentNumber}"/>
                <TextBlock FontSize="12" Foreground="Gray" Text="{Binding Price}"/>
            </StackPanel>
        </StackPanel>

    </StackPanel>

</UserControl>
